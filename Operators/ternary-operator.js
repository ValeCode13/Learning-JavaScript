// If the customer has more than 100 points, 
// they are a 'gold' customer. Otherwise,
// they are 'silver' customer.

// This expression 'points > 100' produces a boolean.

let points = 110;
let type = points > 100 ? 'gold' : 'silver';

console.log('You are a ' + type + ' ' + 'customer');


